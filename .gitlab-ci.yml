# =============================================================================
# BLAUWEISS-EDV LLC – GitLab CI/CD Pipeline (Pandoc + Typst + Quarto)
# =============================================================================
#
# Generiert PDF, DOCX, HTML, TEX aus:
# - Markdown (.md) → Pandoc
# - Typst (.typ) → Typst
# - Quarto (.qmd) und Jupyter (.ipynb) → Quarto
#
# Race-Condition-sicher: Build-Jobs generieren nur, ein Commit-Job pushed alles.
#
# =============================================================================

stages:
  - build
  - commit
  - upload

# =============================================================================
# Job 1: Pandoc (Markdown → PDF, DOCX, HTML, TEX)
# =============================================================================
build-pandoc-docs:
  stage: build
  image: ubuntu:22.04
  
  variables:
    DEBIAN_FRONTEND: noninteractive
    TZ: Europe/Vienna
  
  before_script:
    - apt-get update
    - apt-get install -y --no-install-recommends pandoc texlive-xetex texlive-fonts-recommended texlive-latex-recommended lmodern git ca-certificates
  
  script:
    - echo "📄 Building documents with Pandoc..."
    
    # Legal Formation Documents
    - |
      if [ -d "legal/formation/src" ]; then
        cd legal/formation/src
        for mdfile in *.md; do
          if [ -f "$mdfile" ]; then
            basename="${mdfile%.md}"
            echo "   Building $mdfile → PDF, DOCX, HTML, TEX..."
            pandoc "$mdfile" -o "../dist/${basename}.pdf" --pdf-engine=xelatex -V geometry:margin=2.5cm || echo "   ⚠️ PDF failed for $mdfile"
            pandoc "$mdfile" -o "../dist/${basename}.docx" || echo "   ⚠️ DOCX failed for $mdfile"
            pandoc "$mdfile" -o "../dist/${basename}.html" --standalone || echo "   ⚠️ HTML failed for $mdfile"
            pandoc "$mdfile" -o "../dist/${basename}.tex" || echo "   ⚠️ TEX failed for $mdfile"
          fi
        done
        cd ../../..
      fi
    
    # Invoice Templates
    - |
      if [ -d "finance/templates/invoices/src" ]; then
        cd finance/templates/invoices/src
        for mdfile in *.md; do
          if [ -f "$mdfile" ]; then
            basename="${mdfile%.md}"
            echo "   Building $mdfile → PDF, DOCX, HTML, TEX..."
            pandoc "$mdfile" -o "../dist/${basename}.pdf" --pdf-engine=xelatex -V geometry:margin=2.5cm || echo "   ⚠️ PDF failed for $mdfile"
            pandoc "$mdfile" -o "../dist/${basename}.docx" || echo "   ⚠️ DOCX failed for $mdfile"
            pandoc "$mdfile" -o "../dist/${basename}.html" --standalone || echo "   ⚠️ HTML failed for $mdfile"
            pandoc "$mdfile" -o "../dist/${basename}.tex" || echo "   ⚠️ TEX failed for $mdfile"
          fi
        done
        cd ../../../..
      fi
    
    # Compliance Checklist
    - |
      if [ -f "finance/compliance/04_LLC_Compliance_Checklist.md" ]; then
        cd finance/compliance
        pandoc "04_LLC_Compliance_Checklist.md" -o "04_LLC_Compliance_Checklist.pdf" --pdf-engine=xelatex -V geometry:margin=2.5cm || echo "   ⚠️ PDF failed"
        pandoc "04_LLC_Compliance_Checklist.md" -o "04_LLC_Compliance_Checklist.docx" || echo "   ⚠️ DOCX failed"
        pandoc "04_LLC_Compliance_Checklist.md" -o "04_LLC_Compliance_Checklist.html" --standalone || echo "   ⚠️ HTML failed"
        cd ../..
      fi
    
    - echo "✅ Pandoc build complete!"

  artifacts:
    paths:
      - legal/formation/dist/
      - finance/templates/invoices/dist/
      - finance/compliance/
    expire_in: 1 hour

  rules:
    - if: $CI_COMMIT_MESSAGE =~ /\[skip ci\]/
      when: never
    - if: $CI_PIPELINE_SOURCE == "push"
      changes:
        - "**/*.md"
        - "!**/*.qmd"
    - when: manual
      allow_failure: true

# =============================================================================
# Job 2: Typst (.typ → PDF)
# =============================================================================
build-typst-docs:
  stage: build
  image: ubuntu:22.04
  
  variables:
    DEBIAN_FRONTEND: noninteractive
    TZ: Europe/Vienna
  
  before_script:
    - apt-get update
    - apt-get install -y --no-install-recommends wget git ca-certificates xz-utils
    # Typst installieren
    - wget -qO- https://github.com/typst/typst/releases/latest/download/typst-x86_64-unknown-linux-musl.tar.xz | tar -xJf -
    - mv typst-x86_64-unknown-linux-musl/typst /usr/local/bin/
    - typst --version
  
  script:
    - echo "📄 Building documents with Typst..."
    
    # Alle .typ Dateien finden und kompilieren
    - |
      find . -name "*.typ" -type f | while read typfile; do
        dir=$(dirname "$typfile")
        base=$(basename "$typfile" .typ)
        echo "   Building $typfile → PDF..."
        typst compile "$typfile" "${dir}/${base}.pdf" || echo "   ⚠️ Failed: $typfile"
      done
    
    - echo "✅ Typst build complete!"

  artifacts:
    paths:
      - "**/*.pdf"
    expire_in: 1 hour

  rules:
    - if: $CI_COMMIT_MESSAGE =~ /\[skip ci\]/
      when: never
    - if: $CI_PIPELINE_SOURCE == "push"
      changes:
        - "**/*.typ"
    - when: manual
      allow_failure: true

# =============================================================================
# Job 3: Quarto (.qmd und .ipynb → PDF, HTML, DOCX, TEX)
# =============================================================================
build-quarto-docs:
  stage: build
  image: ubuntu:22.04
  
  variables:
    DEBIAN_FRONTEND: noninteractive
    TZ: Europe/Vienna
  
  before_script:
    - apt-get update
    - apt-get install -y --no-install-recommends wget git ca-certificates xz-utils python3 python3-pip curl texlive-xetex texlive-fonts-recommended texlive-latex-recommended lmodern
    # Typst für Quarto PDF
    - wget -qO- https://github.com/typst/typst/releases/latest/download/typst-x86_64-unknown-linux-musl.tar.xz | tar -xJf -
    - mv typst-x86_64-unknown-linux-musl/typst /usr/local/bin/
    # Quarto installieren
    - wget -qO quarto.deb https://github.com/quarto-dev/quarto-cli/releases/download/v1.6.42/quarto-1.6.42-linux-amd64.deb
    - dpkg -i quarto.deb || apt-get install -f -y
    - quarto --version
    # Python-Pakete für Code-Ausführung
    - pip3 install matplotlib numpy pandas jupyter nbformat
  
  script:
    - echo "📄 Building documents with Quarto..."
    
    # Alle .qmd Dateien rendern
    - |
      find . -name "*.qmd" -type f | while read qmdfile; do
        dir=$(dirname "$qmdfile")
        filename=$(basename "$qmdfile")
        base=$(basename "$qmdfile" .qmd)
        echo "   Building $qmdfile → PDF, HTML, DOCX, TEX..."
        (cd "$dir" && quarto render "$filename" --to typst --output "${base}.pdf") || echo "   ⚠️ PDF failed"
        (cd "$dir" && quarto render "$filename" --to html --output "${base}.html") || echo "   ⚠️ HTML failed"
        (cd "$dir" && quarto render "$filename" --to docx --output "${base}.docx") || echo "   ⚠️ DOCX failed"
        (cd "$dir" && quarto render "$filename" --to latex --output "${base}.tex") || echo "   ⚠️ TEX failed"
      done
    
    # Alle .ipynb Dateien rendern
    - |
      find . -name "*.ipynb" -type f | while read ipynbfile; do
        dir=$(dirname "$ipynbfile")
        filename=$(basename "$ipynbfile")
        base=$(basename "$ipynbfile" .ipynb)
        echo "   Building $ipynbfile → PDF, HTML, DOCX, TEX..."
        (cd "$dir" && quarto render "$filename" --to typst --output "${base}.pdf") || echo "   ⚠️ PDF failed"
        (cd "$dir" && quarto render "$filename" --to html --output "${base}.html") || echo "   ⚠️ HTML failed"
        (cd "$dir" && quarto render "$filename" --to docx --output "${base}.docx") || echo "   ⚠️ DOCX failed"
        (cd "$dir" && quarto render "$filename" --to latex --output "${base}.tex") || echo "   ⚠️ TEX failed"
      done
    
    - echo "✅ Quarto build complete!"

  artifacts:
    paths:
      - "**/*.pdf"
      - "**/*.html"
      - "**/*.docx"
      - "**/*.tex"
    expire_in: 1 hour

  rules:
    - if: $CI_COMMIT_MESSAGE =~ /\[skip ci\]/
      when: never
    - if: $CI_PIPELINE_SOURCE == "push"
      changes:
        - "**/*.qmd"
        - "**/*.ipynb"
    - when: manual
      allow_failure: true

# =============================================================================
# Job 4: Commit & Push (sammelt alle Artifacts und pushed einmal)
# =============================================================================
commit-all:
  stage: commit
  image: ubuntu:22.04
  
  variables:
    DEBIAN_FRONTEND: noninteractive
    TZ: Europe/Vienna
  
  before_script:
    - apt-get update
    - apt-get install -y --no-install-recommends git ca-certificates git-lfs
    - git lfs install --force
    - git config --global user.email "ci@blauweiss-edv.com"
    - git config --global user.name "GitLab CI"
    - git config --global --add safe.directory "$CI_PROJECT_DIR"
  
  script:
    - echo "📦 Committing all generated documents..."
    
    # Debug: Was haben wir?
    - echo "=== Artifacts im Arbeitsverzeichnis ==="
    - find . -name "*.pdf" -o -name "*.html" -o -name "*.docx" | head -30
    
    # Artifacts sichern bevor checkout sie überschreibt
    - mkdir -p /tmp/artifacts
    - cp -r . /tmp/artifacts/ 2>/dev/null || true
    
    # Git für Push konfigurieren
    - git remote set-url origin "https://oauth2:${PROJECT_ACCESS_TOKEN}@gitlab.com/${CI_PROJECT_PATH}.git"
    - git fetch origin $CI_COMMIT_REF_NAME
    - git checkout -f $CI_COMMIT_REF_NAME || git checkout -b $CI_COMMIT_REF_NAME origin/$CI_COMMIT_REF_NAME
    - git pull origin $CI_COMMIT_REF_NAME --rebase || true
    
    # Artifacts zurückkopieren
    - cp -r /tmp/artifacts/legal/ . 2>/dev/null || true
    - cp -r /tmp/artifacts/finance/ . 2>/dev/null || true
    - cp -r /tmp/artifacts/experimental/ . 2>/dev/null || true
    - find /tmp/artifacts -maxdepth 1 \( -name "*.pdf" -o -name "*.html" -o -name "*.docx" -o -name "*.tex" \) -exec cp {} . \; 2>/dev/null || true
    
    - echo "=== Nach Restore ==="
    - git status --short | head -20
    
    - git add -A
    - |
      if git diff --staged --quiet; then
        echo "✅ No changes to commit"
      else
        git status --short | head -20
        git commit -m "🤖 Auto-generate documents [skip ci]"
        git push origin $CI_COMMIT_REF_NAME
        echo "✅ All documents committed and pushed!"
      fi

  rules:
    - if: $CI_COMMIT_MESSAGE =~ /\[skip ci\]/
      when: never
    - when: always

  needs:
    - job: build-pandoc-docs
      optional: true
      artifacts: true
    - job: build-typst-docs
      optional: true
      artifacts: true
    - job: build-quarto-docs
      optional: true
      artifacts: true

# =============================================================================
# Job 5: Upload to Google Drive
# =============================================================================
upload-to-gdrive:
  stage: upload
  image: ubuntu:22.04
  
  variables:
    DEBIAN_FRONTEND: noninteractive
    TZ: Europe/Vienna
  
  before_script:
    - apt-get update
    - apt-get install -y --no-install-recommends curl unzip ca-certificates
    # rclone installieren
    - curl -O https://downloads.rclone.org/rclone-current-linux-amd64.zip
    - unzip rclone-current-linux-amd64.zip
    - cp rclone-*-linux-amd64/rclone /usr/local/bin/
    - rclone --version
  
  script:
    - echo "📤 Uploading documents to Google Drive..."
    
    # Zeige was wir haben
    - echo "=== Files to upload ==="
    - find . \( -name "*.pdf" -o -name "*.docx" -o -name "*.html" \) -type f | head -30
    
    # Upload alle PDFs
    - |
      find . -name "*.pdf" -type f | while read file; do
        echo "   Uploading $file..."
        rclone --config $RCLONE_CONF copy "$file" "gdrive:/BLAUWEISS-EDV-LLC/generated/" || echo "   ⚠️ Failed: $file"
      done
    
    # Upload alle DOCX
    - |
      find . -name "*.docx" -type f | while read file; do
        echo "   Uploading $file..."
        rclone --config $RCLONE_CONF copy "$file" "gdrive:/BLAUWEISS-EDV-LLC/generated/" || echo "   ⚠️ Failed: $file"
      done
    
    - echo "✅ Upload complete!"

  rules:
    - if: $CI_COMMIT_MESSAGE =~ /\[skip ci\]/
      when: never
    - when: always

  needs:
    - job: commit-all
      artifacts: true

